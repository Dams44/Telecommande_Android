<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditText.OutlinedBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:minHeight">55dp</item>
        <item name="android:textSize">17sp</item>
        <item name="font">@font/ubuntu</item>
        <item name="hintEnabled">true</item>
        <item name="android:textColorHint">@color/colorBright</item>
        <item name="shapeAppearanceOverlay">
            @style/ShapeAppearanceOverlay.App.CornerSize15Percent
        </item>
        <item name="boxStrokeWidth">1.5dp</item>
        <item name="boxStrokeColor"> @color/colorPrimary </item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Widget.App.ShapeableButton" parent="Widget.MaterialComponents.ShapeableImageView">
        <item name="shapeAppearanceOverlay">
            @style/ShapeAppearanceOverlay.App.CornerSize20Percent
        </item>
        <item name="strokeWidth"> 1dp </item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
